import { StudioShell } from './studio-shell'

/** Route-facing entry point. Lifecycle state and mode presentation live in StudioShell. */
export function ContextGraphStudioScreen() {
  return <StudioShell />
}
