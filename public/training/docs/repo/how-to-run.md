# Semantier Runtime Runbook

This training page intentionally does not duplicate the operational runbook.

Source of truth: `how-to-run.md` at the repository root.

Keep runtime startup, bootstrap, session artifact, output path, and lakehouse
guidance in the root runbook only.
